OPENAI_API_KEY=sk-teamo-
OPENAI_BASE_URL=https://generativelanguage.googleapis.com/v1beta/openai/
OPENAI_MODEL=gemini-3.8-flash
ALLOWED_ORIGINS=http://localhost:5173
MAX_RESUME_BYTES=8000000
# Demo only: when true, /api/parse-resume returns a fixed demo profile
# instead of parsing the uploaded file with AI. Leave false in production.
USE_MOCK=true
